services:
  - type: web
    name: claim-verifier-pipeline
    runtime: python
    rootDir: pipeline
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn service:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PIPELINE_MODE
        value: mock
